version: "3.9"

services:
  podfree:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./projects:/app/projects
      - ./templates:/app/templates
    restart: unless-stopped
